/**
 * CHECK: Weak Assertions
 *
 * Disabled until the current test-suite backlog is cleaned up and the rule can be enforced.
 */

export function run() {
  return {
    passed: true,
    errors: 0,
    detail: "Disabled",
    messages: [],
  };
}
