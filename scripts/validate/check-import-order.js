/**
 * CHECK: Import Order
 *
 * Disabled because Biome already owns import ordering.
 */

export function run() {
  return {
    passed: true,
    errors: 0,
    detail: "Disabled",
    messages: [],
  };
}
