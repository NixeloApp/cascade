/**
 * CHECK: File Headers
 *
 * Disabled until the existing repo-wide header backlog is intentionally cleaned up.
 */

export function run() {
  return {
    passed: true,
    errors: 0,
    detail: "Disabled",
    messages: [],
  };
}
