/**
 * CHECK: Test Coverage
 *
 * Disabled until missing test coverage is enforced as an intentional repo-wide rule.
 */

export function run() {
  return {
    passed: true,
    errors: 0,
    detail: "Disabled",
    messages: [],
  };
}
