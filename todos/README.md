# Nixelo Todos

Central index for active roadmap and issue todos.

> **Last Audited:** 2026-03-02
> **Structure:** Active todos in `todos/` + open Jules issues in `todos/jules/open/`

---

## Current Focus

| File | Status | Notes |
|------|--------|-------|
| [e2e-reliability-overhaul.md](./e2e-reliability-overhaul.md) | Active | P0 in progress: selector baseline is zero and `e2e-summary` now has local mock-history validation for `history-derived` streak logic |
| [consistency-tracking.md](./consistency-tracking.md) | Active | Ongoing consistency enforcement + validator tracking |
| [feature-gaps.md](./feature-gaps.md) | Active | Core gaps with partial completion |
| [bandwidth_optimization.md](./bandwidth_optimization.md) | Active | High-impact backend efficiency work |
| [oauth-monitoring-finalization.md](./oauth-monitoring-finalization.md) | Active | Monitoring mostly shipped; admin dashboard + feature flag pending |
| [multi-level-views.md](./multi-level-views.md) | Active | Partial route foundation, major features pending |

---

## Priority Lanes

- `Now (P0/P1 security + core):` `e2e-reliability-overhaul`, Jules open issues, `multi-level-views`, `bandwidth_optimization`, `oauth-monitoring-finalization`, `feature-gaps`, `consistency-tracking`
- `Next (P2):` `agency-mvp`, `rich-text-description-followup`, `sidebar-display-limits`, `emoji-overhaul`, `memoization-cleanup`
- `Later (P3/P4):` `public-launch`, `growth-features`, `enterprise`, `uptime-monitoring`

---

## Priority Order

| # | File | Priority Band | Focus |
|---|------|---------------|-------|
| 01 | [e2e-reliability-overhaul.md](./e2e-reliability-overhaul.md) | P0 | Deterministic, robust E2E quality overhaul |
| 02 | [jules/open/jules-sentinel-2026-02-26-issue-search-security.md](./jules/open/jules-sentinel-2026-02-26-issue-search-security.md) | P1 | Search authorization/IDOR hardening |
| 03 | [jules/open/jules-scribe-2024-05-22-fix-cascade-delete-limit.md](./jules/open/jules-scribe-2024-05-22-fix-cascade-delete-limit.md) | P1 | Cascade delete/restore overflow safety |
| 04 | [jules/open/jules-librarian-2026-02-23-lodash-vulnerability.md](./jules/open/jules-librarian-2026-02-23-lodash-vulnerability.md) | P1 | Transitive lodash vulnerability removal |
| 05 | [jules/open/jules-spectra-2025-02-24-deduplicate-hashapikey.md](./jules/open/jules-spectra-2025-02-24-deduplicate-hashapikey.md) | P1 | API key hash test parity cleanup |
| 06 | [multi-level-views.md](./multi-level-views.md) | P1 | Non-project scope views (org/workspace/team) |
| 07 | [bandwidth_optimization.md](./bandwidth_optimization.md) | P1 | Query payload and bandwidth reductions |
| 08 | [oauth-monitoring-finalization.md](./oauth-monitoring-finalization.md) | P1 | OAuth ops dashboard + fail-safe controls |
| 09 | [feature-gaps.md](./feature-gaps.md) | P1 | Core product gaps and Slack integration |
| 10 | [consistency-tracking.md](./consistency-tracking.md) | P1 | Consistency governance closure |
| 11 | [agency-mvp.md](./agency-mvp.md) | P2 | Agency workflow MVP |
| 12 | [rich-text-description-followup.md](./rich-text-description-followup.md) | P2 | Plain-text extraction for integrations |
| 13 | [sidebar-display-limits.md](./sidebar-display-limits.md) | P2 | Sidebar UX follow-ups |
| 14 | [emoji-overhaul.md](./emoji-overhaul.md) | P2 | Icon picker and accessibility |
| 15 | [memoization-cleanup.md](./memoization-cleanup.md) | P2 | React compiler memoization cleanup |
| 16 | [public-launch.md](./public-launch.md) | P3 | Public launch execution |
| 17 | [growth-features.md](./growth-features.md) | P4 | Post-launch growth features |
| 18 | [enterprise.md](./enterprise.md) | P4 | Enterprise roadmap |
| 19 | [uptime-monitoring.md](./uptime-monitoring.md) | P4 | Future uptime/status product track |

---

## Active Todos

| File | Status | Focus |
|------|--------|-------|
| [e2e-reliability-overhaul.md](./e2e-reliability-overhaul.md) | Active | E2E flake elimination and deterministic test architecture |
| [agency-mvp.md](./agency-mvp.md) | Active | Agency workflows (invoicing, client portal) |
| [emoji-overhaul.md](./emoji-overhaul.md) | Active | Icon picker + accessibility pass |
| [sidebar-display-limits.md](./sidebar-display-limits.md) | Active | Core limit work done; UX follow-ups open |
| [rich-text-description-followup.md](./rich-text-description-followup.md) | Active | Plain-text extraction for integrations |
| [memoization-cleanup.md](./memoization-cleanup.md) | Active | React Compiler-driven cleanup |
| [enterprise.md](./enterprise.md) | Active | Enterprise/SSO/infra roadmap |
| [growth-features.md](./growth-features.md) | Active | Growth roadmap with partial shipped items |
| [public-launch.md](./public-launch.md) | Active | Distribution and community launch checklist |
| [uptime-monitoring.md](./uptime-monitoring.md) | Active | Monitor + incident/status-page product area |

---

## Jules Issues (Open)

| File | Priority | Status |
|------|----------|--------|
| [jules/open/jules-sentinel-2026-02-26-issue-search-security.md](./jules/open/jules-sentinel-2026-02-26-issue-search-security.md) | High | Resolved (access-scoped search enforcement + regression coverage validated) |
| [jules/open/jules-scribe-2024-05-22-fix-cascade-delete-limit.md](./jules/open/jules-scribe-2024-05-22-fix-cascade-delete-limit.md) | Medium | Resolved (fanout-safe cascade traversal + overflow regressions validated) |
| [jules/open/jules-librarian-2026-02-23-lodash-vulnerability.md](./jules/open/jules-librarian-2026-02-23-lodash-vulnerability.md) | Medium | Open |
| [jules/open/jules-spectra-2025-02-24-deduplicate-hashapikey.md](./jules/open/jules-spectra-2025-02-24-deduplicate-hashapikey.md) | Low | Resolved (test hash helper deduplicated to production) |

---

## Notes

- `todos/TODO.md` is the lightweight entry file for this index.
- Completed/archive todos were removed per current workflow.
- Use this index as the source of truth for planning and status tracking.
- Priority `01` update (2026-03-02): CI summary reliability now includes deterministic self-tests (`pnpm run e2e:summary:self-test`) with validated fallback contracts for clean (`1/5`), dirty (`0/5`), and flaky-only (`0/5`) runs, exact scan-window accounting (`scanned/limit`), truncation signaling for capped-clean streaks, negative assertions that block false-positive truncation notes in fallback/non-truncated history paths, strict scan-limit parsing for `E2E_STREAK_SCAN_LIMIT` (accepts only canonical positive integers; invalid/non-positive/mixed/ambiguous tokens like `not-a-number`, `0`, `-7`, `250abc`, `010`, and oversized integers fall back to `100`; trimmed valid values remain supported), and a hard safety cap of `1000` runs for valid large scan-limit inputs (`5000` -> `1000`). CI hard rules block `waitForTimeout(...)`, Promise-wrapped `setTimeout` sleeps, `waitForLoadState("networkidle")`, `page.$/page.$$` selector APIs, `force:true` action overrides, XPath locators, and brittle selector regressions with deterministic checker self-tests (`pnpm run e2e:hard-rules:self-test`) wired into CI. Board drag-drop E2E now removes XPath selectors and document-load waits, using test-id/role locators plus `dragTo(...)` post-drag state assertions (`pnpm exec playwright test e2e/board-drag-drop.spec.ts --reporter=line`: `5 passed`). `error-scenarios`, `invite`, `issue-detail-page`, project-tab navigation in `integration-workflow`, `permission-cascade`, onboarding dashboard transitions, and auth integration transitions removed `domcontentloaded` waits in favor of explicit route-outcome checks (`pnpm exec playwright test e2e/error-scenarios.spec.ts --reporter=line`: `4 passed`; `pnpm exec playwright test e2e/invite.spec.ts --reporter=line`: `4 passed`; `pnpm exec playwright test e2e/issue-detail-page.spec.ts --reporter=line`: `3 passed`; `pnpm exec playwright test e2e/integration-workflow.spec.ts --reporter=line`: `4 passed`; `pnpm exec playwright test e2e/permission-cascade.spec.ts --reporter=line`: `9 passed`; `pnpm exec playwright test e2e/onboarding.spec.ts --reporter=line`: `7 passed`; `pnpm exec playwright test e2e/auth.spec.ts --reporter=line`: `19 passed`). `e2e-summary` now has mocked GitHub API-path coverage (not only mock-history fixtures), validating `history-derived` success behavior and explicit `fallback-local` behavior when jobs API responses are non-OK or fetch throws. Validated history contracts cover clean, dirty, flaky-current-run, failing-first latest-run, no-e2e-jobs, and API-path fallback cases (`history-derived` `2/5`; fallback `1/5` / `0/5`), including `3/250`, `1/250`, `2/250`, and `0/250` scan-window behavior plus truncation-case validation (`2/2`). `e2e-summary` emits explicit errors for missing, invalid, and unreadable JSON in both report and mock-history inputs, verifies both step-summary file write/append and stdout output paths, surfaces both timedOut and interrupted outcomes in totals/per-spec heatmap output, and treats timedOut/interrupted as non-clean in fallback checkpoint logic (`0/5`). Latest full-suite rerun remains green (`151 passed`, `0 failed`, `4 skipped`; `0.00%` error on `151` executed tests). Selector baseline debt remains zero. Remaining blocker is one live PR CI validation against real Actions API + step summary output.
- Priority `01` update (2026-03-02, batch BL): `e2e/rbac.spec.ts` removed remaining `waitForLoadState("domcontentloaded")` waits in admin/editor/viewer flows, retaining explicit URL/visibility assertions as synchronization (`pnpm exec playwright test e2e/rbac.spec.ts --reporter=line`: `3 passed`; `pnpm run e2e:hard-rules`: pass, selector baseline `0`). Blocker unchanged: final `history-derived` summary confirmation still requires one live PR CI run.
- Priority `01` update (2026-03-02, batch BM): page-object wait hardening removed remaining weak `domcontentloaded` waits in shared dashboard/workspaces helpers and replaced them with explicit route/UI completion checks (`e2e/pages/dashboard.page.ts`, `e2e/pages/workspaces.page.ts`). A real flake surfaced during validation (`dashboard.filterIssues` tab detachment), then was fixed with retry + active-state guard in the helper. Validation results: `pnpm exec playwright test e2e/teams.spec.ts --reporter=line` => `3 passed`; `pnpm exec playwright test e2e/dashboard.spec.ts --reporter=line` => initial `1 failed` (detachment), rerun after hardening `11 passed`; `pnpm run e2e:hard-rules` => pass with selector baseline `0`. Blocker unchanged: one live PR CI run is still required for final `history-derived` summary confirmation.
- Priority `01` update (2026-03-02, batch BN): RBAC fixture re-auth setup removed shared `waitForLoadState("domcontentloaded")` waits and now gates recovery on explicit dashboard route + navigation landmark readiness (`e2e/fixtures/rbac.fixture.ts`). Validation: `pnpm exec biome check e2e/fixtures/rbac.fixture.ts` => pass; `pnpm run e2e:hard-rules` => pass (selector baseline `0`); `pnpm exec playwright test e2e/rbac.spec.ts --reporter=line` => `3 passed`. Blocker unchanged: one live PR CI run is still required for final `history-derived` summary confirmation.
- Priority `01` update (2026-03-02, batch BO): authenticated/onboarding fixture setup replaced shared `waitForLoadState("domcontentloaded")` waits with explicit destination-route assertions in `e2e/fixtures/auth.fixture.ts` (dashboard and onboarding routes). Validation: `pnpm exec biome check e2e/fixtures/auth.fixture.ts` => pass; `pnpm run e2e:hard-rules` => pass (selector baseline `0`); `pnpm exec playwright test e2e/onboarding.spec.ts --reporter=line` => `7 passed`. Blocker unchanged: one live PR CI run is still required for final `history-derived` summary confirmation.
- Priority `01` update (2026-03-02, batch BP): deprecated shared test helper `waitForNetworkIdle(...)` no longer relies on `waitForLoadState("domcontentloaded")`; it now waits for `document.readyState === "complete"` (`e2e/utils/test-helpers.ts`). Validation: `pnpm exec biome check e2e/utils/test-helpers.ts` => pass; `pnpm run e2e:hard-rules` => pass (selector baseline `0`); `pnpm exec playwright test e2e/rbac.spec.ts --reporter=line` => `3 passed`. Blocker unchanged: one live PR CI run is still required for final `history-derived` summary confirmation.
- Priority `01` update (2026-03-02, batch BQ): auth helper flow removed remaining `waitForLoadState("domcontentloaded")` waits in `e2e/utils/auth-helpers.ts`, switching to dashboard/onboarding URL-settle checks and relying on existing form-state assertions in signup flow. Validation: `pnpm exec biome check e2e/utils/auth-helpers.ts` => pass; `pnpm run e2e:hard-rules` => pass (selector baseline `0`); `pnpm exec playwright test e2e/auth.spec.ts --reporter=line` => `19 passed`. Blocker unchanged: one live PR CI run is still required for final `history-derived` summary confirmation.
- Priority `01` update (2026-03-02, batch BR): shared wait helpers removed remaining `waitForLoadState("domcontentloaded")` usage in `e2e/utils/wait-helpers.ts` (`waitForFormReady` fallback, `waitForReactHydration`, `waitForNavigation`, `waitForDashboardReady`) and standardized on `document.readyState === "complete"` plus explicit UI readiness checks. Validation: `pnpm exec biome check e2e/utils/wait-helpers.ts` => pass; `pnpm run e2e:hard-rules` => pass (selector baseline `0`); `pnpm exec playwright test e2e/dashboard.spec.ts --reporter=line` => `11 passed`; `pnpm exec playwright test e2e/issues.spec.ts --reporter=line` => initial transient `1 failed`/`2 passed`, rerun `3 passed`. Blocker unchanged: one live PR CI run is still required for final `history-derived` summary confirmation.
- Priority `01` update (2026-03-02, batch BS): shared `BasePage` helpers removed remaining `waitForLoadState("domcontentloaded")` usage in `e2e/pages/base.page.ts` (`waitForLoad`, `waitForNavigation` fallback), keeping full-load and React hydration checks as the deterministic contract. Validation: `pnpm exec biome check e2e/pages/base.page.ts` => pass; `pnpm run e2e:hard-rules` => pass (selector baseline `0`); `pnpm exec playwright test e2e/dashboard.spec.ts --reporter=line` => `11 passed`. Blocker unchanged: one live PR CI run is still required for final `history-derived` summary confirmation.
- Priority `01` update (2026-03-02, batch BT): removed the last `waitForLoadState("domcontentloaded")` usage under `e2e/` by updating `e2e/pages/documents.page.ts`, `e2e/pages/auth.page.ts`, `e2e/pages/settings.page.ts`, and `e2e/screenshot-pages.ts` to use `document.readyState === "complete"` readiness checks; repo scan now shows zero remaining `domcontentloaded` waits in `e2e`. Validation: `pnpm exec biome check e2e/pages/documents.page.ts e2e/pages/auth.page.ts e2e/pages/settings.page.ts e2e/screenshot-pages.ts` => pass; `pnpm run e2e:hard-rules` => pass (selector baseline `0`); targeted Playwright runs currently fail on existing auth/bootstrap instability (`documents.spec.ts`: `1 failed`, `3 did not run`; `auth.spec.ts`: `1 failed`, `17 passed`, `1 did not run`; `settings.spec.ts`: no file/tests). Blocker unchanged: sign-in fixture instability plus pending live PR CI validation for final `history-derived` summary confirmation.
- Priority `01` update (2026-03-02, batch BU): hardened shared auth bootstrap flow in `e2e/utils/auth-helpers.ts` by (1) switching API-login route settles to `waitUntil: "load"`, (2) adding targeted `InvalidAccountId` account-repair + one retry via `createTestUser(...)`, and (3) adding `/app` gateway recovery when UI fallback times out on `/signin` or `/`. Validation: `pnpm exec biome check e2e/utils/auth-helpers.ts` => pass; `pnpm run e2e:hard-rules` => pass (selector baseline `0`); `pnpm exec playwright test e2e/auth.spec.ts --reporter=line` => `19 passed`; `pnpm exec playwright test e2e/documents.spec.ts --reporter=line` => `4 passed`. Blocker unchanged: one live PR CI run is still required for final `history-derived` summary confirmation.
- Priority `02` update (2026-03-02, batch A): closed the issue-search IDOR path by enforcing access scoping in `convex/issues/queries.ts` for all `issues.search` branches. Unauthorized `projectId` queries now return empty immediately, and all fetched candidate issues are filtered by `canAccessProject(...)` before `matchesSearchFilters(...)`. Regression coverage added in `convex/issues.test.ts` proves: (1) global text search does not leak inaccessible project issues, and (2) unauthorized `projectId` and `organizationId` scoped searches return empty. Validation: `pnpm exec biome check convex/issues/queries.ts convex/issues.test.ts` => pass; `pnpm test convex/issues.test.ts` => `28 passed`.
- Priority `03` update (2026-03-02, batch A): resolved cascade fanout overflow behavior in `convex/lib/relationships.ts`. `cascadeDelete`, `set_null`, `cascadeSoftDelete`, and `cascadeRestore` now process children in iterative bounded loops until exhausted (instead of throwing when child count exceeds `BOUNDED_DELETE_BATCH`). Soft-delete and restore loops are scoped to pending rows only (`isDeleted !== true` for soft-delete, `isDeleted === true` for restore) to avoid reprocessing. Regression coverage in `convex/lib/relationships_overflow.test.ts` now validates `BOUNDED_DELETE_BATCH + 1` fanout for hard delete, soft delete, and restore. Validation: `pnpm test convex/lib/relationships.test.ts convex/lib/relationships_overflow.test.ts` => `7 passed`.
- Priority `04` update (2026-03-02, batch A): audited lodash vulnerability path and confirmed it remains `@boxyhq/saml-jackson@1.52.2 -> lodash@4.17.21` (`pnpm why lodash`). Upgrade work is currently blocked by environment DNS outage: `pnpm --store-dir /home/mikhail/.local/share/pnpm/store/v10 up @boxyhq/saml-jackson@latest` fails with `ERR_PNPM_META_FETCH_FAIL` / `getaddrinfo EAI_AGAIN registry.npmjs.org`. No code or lockfile changes applied; this item stays open until registry access is restored and upgrade/audit can be rerun.
- Priority `05` update (2026-03-02, batch A): removed duplicate API key hashing logic from `convex/apiKeys.test.ts` and switched tests to reuse production `hashApiKey` from `convex/lib/apiAuth.ts`. All key-hash validation calls in tests now await the shared async helper, keeping test behavior aligned with runtime implementation. Validation: `pnpm exec biome check convex/apiKeys.test.ts` => pass; `pnpm test convex/apiKeys.test.ts` => `13 passed`.
- Priority `06` update (2026-03-02, batch A): completed `multi-level-views` milestone `S1` schema blockers. Added scope fields/indexes for multi-level documents/calendar (`documents.teamId`; `calendarEvents.organizationId/workspaceId/teamId`), expanded search-index filter fields, and wired scope propagation in mutations (`convex/documents.ts` create/team access validation, `convex/calendarEvents.ts` scope derivation from project/issue on create/update). Validation: `pnpm test convex/calendarEvents.test.ts convex/documents.test.ts` => `51 passed`; `pnpm exec biome check convex/schema.ts convex/documents.ts convex/calendarEvents.ts` => pass with non-blocking complexity warning in `canAccessDocument`. Next step remains `S2` workspace backlog route/query.
- Priority `06` update (2026-03-02, batch B): completed workspace backlog slice of `S2`. Added `workspaces.getBacklogIssues` query in `convex/workspaces.ts`, introduced `ROUTES.workspaces.backlog` in `convex/shared/routes.ts`, added route `src/routes/_auth/_app/$orgSlug/workspaces/$workspaceSlug/backlog.tsx`, and wired Backlog tab nav in workspace layout route. Validation: `pnpm exec biome check convex/shared/routes.ts convex/workspaces.ts src/routes/_auth/_app/$orgSlug/workspaces/$workspaceSlug/route.tsx src/routes/_auth/_app/$orgSlug/workspaces/$workspaceSlug/backlog.tsx` => pass; `pnpm run typecheck` => pass; `pnpm test convex/workspaces.test.ts` => `34 passed`.
- Priority `06` update (2026-03-02, batch C): completed workspace sprints slice and closed `S2`. Added `workspaces.getActiveSprints` query in `convex/workspaces.ts` (active sprints across workspace projects with issue counts), introduced `ROUTES.workspaces.sprints`, added route `src/routes/_auth/_app/$orgSlug/workspaces/$workspaceSlug/sprints.tsx`, and wired Sprints tab nav in workspace layout. Validation: `pnpm exec biome check convex/shared/routes.ts convex/workspaces.ts src/routes/_auth/_app/$orgSlug/workspaces/$workspaceSlug/route.tsx src/routes/_auth/_app/$orgSlug/workspaces/$workspaceSlug/sprints.tsx` => pass; `pnpm run typecheck` => pass; `pnpm test convex/workspaces.test.ts` => `34 passed`.
- Priority `06` update (2026-03-02, batch D): completed workspace/team wiki slice and closed `S3`. Added scoped document queries (`api.documents.listByWorkspace`, `api.documents.listByTeam`) in `convex/documents.ts`, added workspace wiki route `src/routes/_auth/_app/$orgSlug/workspaces/$workspaceSlug/wiki.tsx`, added team wiki route `src/routes/_auth/_app/$orgSlug/workspaces/$workspaceSlug/teams/$teamSlug/wiki.tsx`, added `ROUTES.workspaces.teams.wiki`, and wired Wiki tabs in workspace/team layouts. Validation: `pnpm run typecheck` => pass; `pnpm test convex/documents.test.ts convex/workspaces.test.ts` => `62 passed`.
- Priority `06` update (2026-03-02, batch E): completed `S4` by replacing the team calendar stub with real team-scoped data. Added `api.calendarEvents.listByTeamDateRange` in `convex/calendarEvents.ts` (team member/org admin access), wired `CalendarView` scope props so team routes use team-scoped events and project routes pass `projectId`, replaced `src/routes/_auth/_app/$orgSlug/workspaces/$workspaceSlug/teams/$teamSlug/calendar.tsx` with the live calendar page, and added regression coverage in `convex/calendarEvents.test.ts` for member access + non-member exclusion. Validation: `pnpm exec biome check convex/calendarEvents.ts convex/calendarEvents.test.ts src/components/Calendar/CalendarView.tsx src/components/Calendar/ProjectCalendar.tsx src/routes/_auth/_app/$orgSlug/workspaces/$workspaceSlug/teams/$teamSlug/calendar.tsx` => pass; `pnpm run typecheck` => pass; `pnpm test convex/calendarEvents.test.ts` => pass.
- Priority `06` update (2026-03-02, batch F): partial `S5` shipped: added scoped calendar aggregation + filters for organization/workspace views. Backend adds `api.calendarEvents.listByOrganizationDateRange` and `api.calendarEvents.listByWorkspaceDateRange` with membership checks and optional scope filters; frontend adds org calendar route `src/routes/_auth/_app/$orgSlug/calendar.tsx`, workspace calendar route `src/routes/_auth/_app/$orgSlug/workspaces/$workspaceSlug/calendar.tsx`, `ROUTES.calendar`, `ROUTES.workspaces.calendar`, workspace calendar tab wiring, and sidebar “General” navigation to org calendar; `CalendarView` now selects query source by scope (`org/workspace/team/project/user`). Regression coverage added for new calendar queries and route constants (`convex/calendarEvents.test.ts`, `src/config/routes.test.ts`). Validation: `pnpm run generate:routes` => pass; `pnpm run typecheck` => pass; `pnpm test convex/calendarEvents.test.ts src/config/routes.test.ts` => `63 passed`. Remaining `S5` gap: workspace/team color-coding in org/workspace calendar.
- Priority `06` update (2026-03-02, batch G): closed the remaining `S5` gap by adding deterministic workspace/team color-coding in scoped calendar views. `CalendarView` now supports `colorByScope` and assigns palette colors by hashed scope ID; org/workspace calendar routes now select color mode by active scope filters (org-wide -> workspace colors, workspace-wide -> team colors). Validation: `pnpm run typecheck` => pass; `pnpm test convex/calendarEvents.test.ts src/config/routes.test.ts` => `63 passed`. `S5` is complete; remaining Priority `06` work is now dependencies/personal-board tracks.
- Priority `06` update (2026-03-02, batch H): shipped workspace cross-team dependencies route foundation. Added `api.workspaces.getCrossTeamDependencies` (cross-team `blocks` links with `team/status/priority` filters), new route constant `ROUTES.workspaces.dependencies`, new page `src/routes/_auth/_app/$orgSlug/workspaces/$workspaceSlug/dependencies.tsx`, and workspace `Dependencies` tab wiring; added regressions in `convex/workspaces.test.ts` and route constant tests in `src/config/routes.test.ts`; regenerated route tree (`src/routeTree.gen.ts`). Validation: `pnpm run generate:routes` => pass; `pnpm run typecheck` => pass; `pnpm test convex/workspaces.test.ts src/config/routes.test.ts` => `73 passed`. Remaining gap for this track: replace list-based dependencies UI with required `react-flow` visualization.
- Priority `06` update (2026-03-02, batch I): shipped Personal Board route. Added `ROUTES.myIssues`, new page `src/routes/_auth/_app/$orgSlug/my-issues.tsx`, sidebar `My Board` nav item, and board grouping toggle (`status`/`project`) over paginated `api.dashboard.getMyIssues` data with load-more support. Route tree regenerated (`src/routeTree.gen.ts`), and validation passed: `pnpm run generate:routes`, `pnpm run typecheck`, `pnpm test src/config/routes.test.ts src/components/App/AppSidebar.test.ts` (`46 passed`). Remaining Priority `06` gap is dependencies graph visualization (`react-flow`).
- Priority `06` update (2026-03-02, batch J): attempted to unblock the remaining dependencies graph requirement by installing `@xyflow/react`, but environment blocked dependency fetch/install (`ERR_PNPM_UNEXPECTED_STORE` + `ERR_PNPM_META_FETCH_FAIL` with `getaddrinfo EAI_AGAIN registry.npmjs.org`). No graph implementation landed yet; blocker is registry/network/store configuration. Also retained standards-compliant `my-issues` layout (`Flex`/`FlexItem`) after pre-commit stash/restore.
- Priority `07` update (2026-03-02, batch A): reduced dashboard bandwidth by minimizing `api.dashboard.getMyIssues` payload shape to only fields consumed by dashboard/command palette, avoiding transfer of large issue fields (e.g., descriptions/attachments/labels arrays). Also aligned `getMyCreatedIssues` with `reporterName` for feed merge type consistency and updated command palette recent-issues typing. Validation: `pnpm run typecheck` => pass; `pnpm test convex/dashboard.test.ts` => `29 passed`.
- Priority `07` update (2026-03-02, batch B): removed global activity scan from `api.dashboard.getMyRecentActivity`. The query now scopes to member projects (`projectMembers.by_user`), samples recent project issues (`issues.by_project_deleted`), fetches activity only for those issues (`issueActivity.by_issue`), then merges/sorts/enriches results. Added regression in `convex/dashboard.test.ts` confirming non-member project activity is excluded. Validation: `pnpm run typecheck` => pass; `pnpm test convex/dashboard.test.ts` => `30 passed`. `S1` (dashboard/activity scoping + payload minimization) is now complete in `bandwidth_optimization`.
- Priority `07` update (2026-03-02, batch C): optimized `api.analytics.getProjectAnalytics` to eliminate full-issue fetches for aggregate metrics. The query now uses bounded index-backed counts (`efficientCount`) for totals, status/type/priority buckets, unassigned count, and per-assignee counts; output shape remains unchanged. Added new issues index `by_project_priority_deleted` in `convex/schema.ts` for priority aggregation and adjusted analytics test fixture membership in `convex/analytics.test.ts`. Validation: `pnpm run typecheck` => pass; `pnpm test convex/analytics.test.ts` => `26 passed`. Next step in Priority `07`: add query-level payload telemetry for top bandwidth endpoints (`S1` telemetry milestone).
- Priority `07` update (2026-03-02, batch D): completed `S1` telemetry milestone by adding query-level payload telemetry for top bandwidth endpoints. New helper `convex/lib/payloadTelemetry.ts` estimates JSON payload bytes and logs structured telemetry behind `ENABLE_QUERY_PAYLOAD_TELEMETRY` (`1`/`true`), with automatic suppression in test env. Instrumented `dashboard.getMyIssues`, `dashboard.getMyRecentActivity`, and `analytics.getProjectAnalytics`; added tests in `convex/lib/payloadTelemetry.test.ts`. Validation: `pnpm run typecheck` => pass; `pnpm test convex/dashboard.test.ts convex/analytics.test.ts convex/lib/payloadTelemetry.test.ts` => `59 passed`. Next Priority `07` step is `S2` counters/stats table work.
- Priority `07` update (2026-03-02, batch E): completed `S2` counters-table implementation for high-frequency project issue totals. Added new `projectIssueStats` table (`projectId`, `totalIssues`, `updatedAt`) with `by_project` index and helper `convex/lib/projectIssueStats.ts` (`syncProjectIssueStats`, `getProjectIssueCounts`). Wired issue-count sync into `createIssue` and `bulkDelete` mutation flows, and updated `dashboard.getMyProjects` to return real `totalIssues` from cached/fallback counts instead of `0`. Added regressions in `convex/dashboard.test.ts` for create + soft-delete count correctness. Validation: `pnpm run typecheck` => pass; `pnpm test convex/dashboard.test.ts convex/issues/mutations.test.ts` => `55 passed`. Remaining Priority `07` item: publish before/after bandwidth report from Convex metrics.
- Priority `07` update (2026-03-02, batch F): attempted to complete the final bandwidth report publication item, but it is currently blocked by metrics access. Verified CLI capabilities (`pnpm exec convex --help`) and confirmed no bandwidth/usage metrics export command exists; `convex dashboard` is browser/UI-based and unavailable in this headless workflow. Stability validation remains green after optimizations (`pnpm run test:run` => pass, `pnpm run test:all` => pass). Blocker for closure: operator must capture before/after bandwidth numbers from the Convex dashboard UI and feed them into `todos/bandwidth_optimization.md`.
- Priority `08` update (2026-03-02, batch A): completed OAuth monitoring `S1` admin visibility work. Added admin-only `api.oauthHealthCheck.getOAuthHealthStats` (7d/30d-ready windowed stats with success rate, average/p95 latency, consecutive failures, recent failures, and incident summary fields `firstFailAt` / `lastFailAt` / `recoveredAt`), integrated new admin settings surface `src/components/Admin/OAuthHealthDashboard.tsx`, and mounted it in `src/components/Settings.tsx` admin tab. Added backend tests for admin access and non-admin rejection in `convex/oauthHealthCheck.test.ts`. Validation: `pnpm run typecheck` => pass; `pnpm test convex/oauthHealthCheck.test.ts` => `14 passed`. Next Priority `08` step: S2 feature-flag controls for emergency Google auth disable.
- Priority `08` update (2026-03-02, batch B): completed S2 emergency feature-flag controls for Google auth. Added new `featureFlags` table and API (`api.featureFlags.isGoogleAuthEnabled`, `api.featureFlags.setGoogleAuthEnabled`), wired auth-page fallback in `src/components/Auth/GoogleAuthButton.tsx` (disabled guidance state when flag is off), and added admin toggle UI in `src/components/Admin/OAuthFeatureFlagSettings.tsx` (mounted in settings admin tab). Added backend coverage in `convex/featureFlags.test.ts` and updated purge coverage (`convex/purge.ts`). Validation: `pnpm run typecheck` => pass; `pnpm test convex/featureFlags.test.ts convex/oauthHealthCheck.test.ts` => `17 passed`. Remaining Priority `08` item: runbook hardening for emergency disable/re-enable verification checklist.
- Priority `08` update (2026-03-02, batch C): completed runbook hardening in `docs/oauth-monitoring-runbook.md` with a full emergency Google-auth kill-switch procedure: disable checklist, disable verification steps, re-enable checklist, and post-recovery verification steps. Updated `todos/oauth-monitoring-finalization.md` to mark Phase 5 runbook documentation and S2 runbook-hardening milestone complete. Remaining optional scope in Priority `08`: external monitoring integration beyond current Convex + Slack setup.
- Priority `08` update (2026-03-02, batch D): classified external monitoring push integration as blocked optional scope pending a concrete DataDog/Grafana destination + owner. `todos/oauth-monitoring-finalization.md` now marks status as blocked for that specific decision, while core OAuth monitoring path (health checks, admin dashboard, kill switch, and Slack alerting) is complete. Next strict-order move: proceed to Priority `09` until external destination details are provided.
- Priority `09` update (2026-03-02, batch A): started `feature-gaps` `S1` rich-text parity by adding comment emoji picker support in `src/components/MentionInput.tsx`. Comment composer now has an emoji toolbar popover with cursor-aware insertion and caret restoration, while preserving mention extraction behavior. Validation: `pnpm run typecheck` => pass; `pnpm test src/components/CommentReactions.test.tsx` => `2 passed`. Next Priority `09` step: inline comment file attachments.
- Priority `09` update (2026-03-02, batch B): completed `feature-gaps` `S1` by shipping inline comment file attachments. Added optional `issueComments.attachments` storage IDs in schema, extended `api.issues.addComment` to accept attachment IDs with issue-membership enforcement, and updated `src/components/IssueComments.tsx` to support inline file upload/removal in composer plus inline attachment rendering per comment. Validation: `pnpm exec biome check --write convex/schema.ts convex/issues/mutations.ts convex/issues/mutations.test.ts src/components/IssueComments.tsx` => pass (non-blocking complexity warning); `pnpm run typecheck` => pass; `pnpm test convex/issues/mutations.test.ts` => `24 passed`. Residual gap: `convex-test` currently cannot generate valid `_storage` IDs for dedicated unit coverage of attachment-membership guard, so that branch remains covered by runtime logic/E2E. Next strict-order step: continue Priority `09` with `S2` Slack outbound notifications MVP.
- Priority `09` update (2026-03-02, batch C): progressed `feature-gaps` `S2` with Slack OAuth and connection infrastructure. Added `slackConnections` schema/table, `convex/slack.ts` (`connectSlack`/`getConnection`/`disconnectSlack`), Slack OAuth HTTP handlers (`/slack/auth`, `/slack/callback`) in `convex/http/slackOAuth.ts`, router wiring in `convex/router.ts`, env helpers for `SLACK_CLIENT_ID`/`SLACK_CLIENT_SECRET`, new integrations UI card `src/components/Settings/SlackIntegration.tsx`, and settings-tab integration. Added purge table coverage and backend tests (`convex/slack.test.ts`: `3 passed`). Validation: `pnpm exec biome check --write ...` => pass; `pnpm run typecheck` => pass; `pnpm test convex/slack.test.ts` => pass. Remaining blockers: external Slack app provisioning is manual, and outbound issue-event delivery to Slack channels is still pending.
- Priority `09` update (2026-03-02, batch D): completed local Slack outbound delivery wiring for `S2`. `convex/slack.ts` now includes authenticated `sendMessage`, internal delivery action, internal `sendIssueNotification` fanout, issue/destination internal queries, delivery stats updates, and webhook URL validation; `convex/schema.ts` extends `slackConnections` with optional delivery stats. `convex/issues/mutations.ts` now triggers Slack events for `issue.created`, `issue.updated`, `issue.assigned`, and `comment.created`, with scheduler pre-checks so jobs are only enqueued when project Slack destinations exist. Validation: `pnpm exec biome check --write convex/slack.ts convex/slack.test.ts convex/issues/mutations.ts convex/schema.ts` => pass; `pnpm run typecheck` => pass; `pnpm test convex/slack.test.ts` => `6 passed`; `pnpm test convex/issues/mutations.test.ts` => `24 passed`. Remaining blocker for full `S2` closure is external/manual Slack app provisioning + env setup in Slack dashboard.
- Priority `09` update (2026-03-02, batch E): implemented slash-command backend support for `S2` follow-through. Added Slack command HTTP endpoint (`POST /slack/commands`) in `convex/http/slackCommands.ts` with Slack signature validation using `SLACK_SIGNING_SECRET`; added command core executor in `convex/slackCommandsCore.ts` supporting `create`, `search`, and `assign`; wired route in `convex/router.ts`; added signing-secret env helpers in `convex/lib/env.ts`; and added command flow tests in `convex/slackCommands.test.ts` (`create/search/assign`). Validation: `pnpm run typecheck` => pass; `pnpm test convex/slackCommands.test.ts convex/slack.test.ts` => `9 passed`; `pnpm test convex/issues/mutations.test.ts` => `24 passed`. Remaining blockers: manual Slack dashboard registration for `/nixelo` plus env setup for `SLACK_SIGNING_SECRET`.
- Priority `09` update (2026-03-02, batch F): implemented Slack link-unfurl backend for `S3` readiness. Added internal issue-unfurl resolver `convex/slackUnfurl.ts` (team-context resolution, issue-key extraction, access checks via `canAccessProject`, metadata payload), added signed HTTP endpoint `POST /slack/unfurl` in `convex/http/slackUnfurl.ts`, and wired route in `convex/router.ts`. Added tests in `convex/slackUnfurl.test.ts` for positive and null paths. Validation: `pnpm run typecheck` => pass; `pnpm test convex/slackCommands.test.ts convex/slackUnfurl.test.ts convex/slack.test.ts` => `11 passed`; `pnpm test convex/issues/mutations.test.ts` => `24 passed`. Remaining blocker for complete Slack integration closure is manual Slack dashboard registration (slash command + URL unfurl patterns) and production env setup.
- Priority `09` update (2026-03-02, batch G): classified Priority `09` as externally blocked after completing all repository-side Slack integration work. Remaining open items require manual Slack API dashboard actions (OAuth app creation, `/nixelo` registration, URL pattern registration) and production env setup (`SLACK_CLIENT_ID`, `SLACK_CLIENT_SECRET`, `SLACK_SIGNING_SECRET`). This blocker status allows strict-order progression to Priority `10`.
- Priority `10` update (2026-03-02, batch A): closed the remaining process baseline in `consistency-tracking` and transitioned it to operational mode. Added PR checklist enforcement for new/updated non-trivial file headers in `.github/PULL_REQUEST_TEMPLATE.md`, marked the final unchecked action item complete, added an explicit monthly consistency snapshot cadence, and documented validator-inventory sync rules. Validation: docs/process-only batch (no runtime code changes). Next strict-order step: proceed to Priority `11` (`agency-mvp.md`).
- Priority `09` update (2026-03-02, batch G): marked `feature-gaps` as externally blocked for final Slack completion. All repository-side work for Slack integration (`S2/S3`) is implemented; remaining unchecked items require manual Slack API dashboard actions (OAuth app creation, `/nixelo` command registration, URL unfurl pattern registration) plus production env provisioning (`SLACK_CLIENT_ID`, `SLACK_CLIENT_SECRET`, `SLACK_SIGNING_SECRET`). No code/test changes in this batch; this is a strict-order blocker classification to allow progression to Priority `10` until operator unblocks Slack dashboard steps.
- Priority `11` update (2026-03-02, batch A): completed `agency-mvp` milestone `S1` (schema + backend foundation). Added new `clients` and `invoices` tables in `convex/schema.ts` with billing-oriented indexes, wired purge support in `convex/purge.ts`, implemented org-scoped base CRUD in `convex/clients.ts` and `convex/invoices.ts` (including invoice number generation, line-item total calculation, and client-organization guardrails), and added regressions in `convex/clients.test.ts` + `convex/invoices.test.ts` (7 passing tests). Validation: `pnpm run typecheck` => pass; `pnpm test convex/clients.test.ts convex/invoices.test.ts` => pass. Decision: kept `S1` strictly to backend foundation; deferred time-entry generation/send/paid/PDF lifecycle work to `S2`. Blocker: Convex codegen is currently network-failing in this environment (`fetch failed`), so tests use `anyApi` function references until codegen connectivity is restored. Next strict-order step: continue Priority `11` with `S2` invoice lifecycle (`generateFromTimeEntries`, `send`, `markPaid`) implementation.
- Priority `11` update (2026-03-02, batch B): advanced `agency-mvp` `S2` lifecycle work by implementing `api.invoices.generateFromTimeEntries`, `api.invoices.send`, and `api.invoices.markPaid` in `convex/invoices.ts`. Auto-generation now collects unbilled billable entries in range (org/project scoped), creates draft invoices with computed line-item totals, and links source entries as billed; lifecycle transitions enforce draft->sent->paid guardrails. Added coverage in `convex/invoices.test.ts` for generation, lifecycle transitions, and linked-entry billing updates; suite now reports `8/8` passing tests across `clients` and `invoices`. Validation: `pnpm run typecheck` => pass; `pnpm test convex/clients.test.ts convex/invoices.test.ts` => pass. Remaining Priority `11` blocker in `S2`: PDF generation path (`generatePdf`) and send-channel wiring (email provider/template + storage strategy).
- Priority `11` update (2026-03-02, batch C): closed remaining `S2` gaps by adding `api.invoices.generatePdf` (admin-only action) and send-channel wiring in `api.invoices.send`. PDF generation now builds a deterministic invoice summary PDF blob server-side, stores it in Convex storage, and persists `invoice.pdfUrl`; send now enforces client-email presence and queues `internal.email.index.sendEmailAction` with invoice subject/html/text payload after status transitions to `sent`. Added regression coverage for admin/member PDF access control and PDF URL persistence in `convex/invoices.test.ts`; overall billing backend tests now pass `9/9`. Validation: `pnpm run typecheck` => pass; `pnpm test convex/clients.test.ts convex/invoices.test.ts` => pass. Decision: used minimal server-side PDF template without new dependencies due current network/codegen instability. Next strict-order step: continue Priority `11` with `S3` UI implementation (invoice/client routes + editor components).
- Priority `11` update (2026-03-02, batch D): completed `S3` UI foundation for agency billing. Added route constants (`ROUTES.invoices.list/detail`, `ROUTES.clients.list`) in `convex/shared/routes.ts`; added frontend routes `src/routes/_auth/_app/$orgSlug/invoices/index.tsx`, `src/routes/_auth/_app/$orgSlug/invoices/$invoiceId.tsx`, and `src/routes/_auth/_app/$orgSlug/clients/index.tsx`; added reusable invoice components `src/components/Invoices/InvoiceEditor.tsx` and `src/components/Invoices/InvoicePdfTemplate.tsx`; wired sidebar navigation entries in `src/components/App/AppSidebar.tsx`; and regenerated route tree (`src/routeTree.gen.ts`). Validation: `pnpm run generate:routes` => pass; `pnpm run typecheck` => pass; `pnpm test src/config/routes.test.ts src/components/App/AppSidebar.test.ts` => `48 passed`. Remaining Priority `11` work now centers on `S4` client portal backend + routes (`clientPortalTokens`, token lifecycle, portal pages).
- Priority `11` update (2026-03-02, batch E): advanced `S4` client portal work with backend token lifecycle and initial portal scaffolding. Added `clientPortalTokens` schema + indexes in `convex/schema.ts`, purge coverage in `convex/purge.ts`, new portal validation limiter in `convex/rateLimits.ts`, and new `convex/clientPortal.ts` API (`generateToken`, `validateToken`, `getProjectsForToken`, `getIssuesForToken`, `revokeToken`, `listTokensByClient`) with org/client/project scope enforcement, expiry/revocation handling, and access timestamp updates. Added backend tests in `convex/clientPortal.test.ts` (`3 passed`). Added first-pass portal UI scaffolding via `src/routes/portal.$token.tsx`, `src/routes/portal.$token.projects.$projectId.tsx`, and `src/components/ClientPortal/{PortalHeader,PortalProjectView,PortalTimeline}.tsx`; regenerated `src/routeTree.gen.ts`. Validation: `pnpm run generate:routes` => pass; `pnpm run typecheck` => pass; `pnpm test convex/clientPortal.test.ts` => `3 passed`; `pnpm test src/config/routes.test.ts` => `40 passed`. Remaining S4 gap: wire portal routes to live `clientPortal` APIs and add token-management UI in client settings.
- Priority `11` update (2026-03-02, batch F): completed remaining S4 frontend integration and closed Priority 11 core scope. Portal routes now call live client-portal functions (token validation + scoped projects/issues), and client-management UI now includes portal-link generation, token listing, and revocation controls. Validation remained green: `pnpm run generate:routes` => pass; `pnpm run typecheck` => pass; `pnpm test convex/clientPortal.test.ts src/config/routes.test.ts` => `43 passed`. Decision: token management was implemented in the client-management surface instead of a dedicated settings sub-tab for faster operator workflow. Priority `11` is now complete for MVP scope; next strict-order step is Priority `12` (`rich-text-description-followup.md`).
- Priority `12` update (2026-03-02, batch A): completed rich-text follow-up by adding backend plain-text extraction helper `convex/lib/richText.ts` (`getPlainTextFromDescription`) and wiring it into `convex/pumble.ts` (notification body text), `convex/ai/chat.ts` (`generateIssueEmbedding` input text), `convex/export.ts` (CSV `Description` column + plain-text JSON export descriptions), and `convex/issues/helpers.ts` (`getSearchContent` normalization for indexing). Added regressions in `convex/lib/richText.test.ts`, `convex/pumble.test.ts`, `convex/export.test.ts`, and `convex/issues/helpers.test.ts`. Validation: `pnpm exec biome check --write ...` => pass; `pnpm test convex/lib/richText.test.ts convex/issues/helpers.test.ts convex/export.test.ts convex/pumble.test.ts` => `86 passed`; `pnpm run typecheck` => pass. Blockers: none. Next strict-order step: Priority `13` (`sidebar-display-limits.md`).
- Priority `13` update (2026-03-02, batch A): completed sidebar display-limit UX follow-ups in `src/components/App/AppSidebar.tsx`. Added conditional sidebar search triggers (`Search documents` when docs > `10`, `Search workspaces` when workspaces > `25`) and explicit route-based overflow affordances (`Show all documents (N)` -> documents list, `Show all workspaces (N)` -> workspaces list). Added workspace empty-filter messaging and forwarded `onClick` in `NavSubItem` to preserve mobile-close behavior on sub-route navigation. Regression coverage added in `src/components/App/AppSidebar.test.tsx` for both document/workspace overflow search + show-all behavior. Validation: `pnpm exec biome check --write src/components/App/AppSidebar.tsx src/components/App/AppSidebar.test.tsx` => pass (non-blocking complexity warning in `AppSidebar`); `pnpm test src/components/App/AppSidebar.test.tsx src/config/routes.test.ts` => `50 passed`; `pnpm run typecheck` => pass. Blockers: none. Next strict-order step: Priority `14` (`emoji-overhaul.md`).
- Priority `14` update (2026-03-02, batch A): completed `S2` icon-picker UX rollout. Added reusable `src/components/ui/IconPicker.tsx` with 35 curated Lucide options, search/filter, hover preview, keyboard grid navigation, and emoji fallback options; added `TemplateIcon` renderer to support both `lucide:<IconName>` and legacy emoji values. Integrated picker into `src/components/Documents/DocumentTemplatesManager.tsx`, replacing raw emoji text input and removing the old `maxLength(2)` icon restriction while preserving existing emoji compatibility. Added tests in `src/components/ui/IconPicker.test.tsx` for filtering, selection, keyboard navigation, and fallback rendering. Validation: `pnpm exec biome check --write src/components/ui/IconPicker.tsx src/components/ui/IconPicker.test.tsx src/components/Documents/DocumentTemplatesManager.tsx` => pass; `pnpm test src/components/ui/IconPicker.test.tsx src/components/App/AppSidebar.test.tsx` => `15 passed`; `pnpm run typecheck` => pass. Remaining Priority `14` scope is `S3` structured icon schema migration + migration script + accessibility audit.
- Priority `14` update (2026-03-02, batch B): completed the `S3` structured icon schema/migration slice. `convex/schema.ts` now supports structured `documentTemplates.icon` objects (`lucide`/`emoji`) with transitional legacy-string compatibility; `convex/documentTemplates.ts` now normalizes create/update icon inputs to structured storage, serializes query output for frontend compatibility, and normalizes built-in template initialization. Added migration mutation `internal.documentTemplates.migrateLegacyIconStrings` to backfill legacy string rows in batches, plus regression coverage in `convex/documentTemplates.test.ts` for structured storage + migration behavior. Validation: `pnpm exec biome check --write convex/documentTemplates.ts convex/documentTemplates.test.ts convex/schema.ts src/components/ui/IconPicker.tsx src/components/Documents/DocumentTemplatesManager.tsx` => pass; `pnpm test convex/documentTemplates.test.ts src/components/ui/IconPicker.test.tsx` => `29 passed`; `pnpm run typecheck` => pass. Remaining Priority `14` scope is Phase 3 emoji usage audits and Phase 4 accessibility audit.
- Priority `14` update (2026-03-02, batch C): completed Phase 3 audit pass and partial Phase 4 hardening. Audit results: workspace/team/project-template icons still use emoji/string schemas (`workspaces.icon`, `teams.icon`, `projectTemplates.icon`) with workspace settings on curated emoji selector; no dedicated custom-field emoji-icon schema or user-status emoji schema requiring migration was found. Accessibility hardening added `aria-label` coverage for icon-only edit/delete template buttons in `src/components/Documents/DocumentTemplatesManager.tsx`. Validation: `pnpm test convex/documentTemplates.test.ts src/components/ui/IconPicker.test.tsx` => `29 passed`; `pnpm run typecheck` => pass. Remaining Priority `14` scope is manual WCAG contrast + screen-reader verification for final closure.
- Priority `15` update (2026-03-02, batch A): started memoization cleanup with a conservative pure-derivation batch. Removed `useMemo` from `src/components/Invoices/InvoiceEditor.tsx`, `src/components/Sprints/SprintProgressBar.tsx`, `src/components/Analytics/LineChart.tsx`, `src/routes/_auth/_app/$orgSlug/my-issues.tsx`, `src/routes/_auth/_app/$orgSlug/workspaces/$workspaceSlug/dependencies.tsx`, and `src/components/ui/IconPicker.tsx`. Validation: `pnpm exec biome check --write ...` => pass (non-blocking complexity warning retained in `SprintProgressBar`); `pnpm test src/components/ui/IconPicker.test.tsx src/config/routes.test.ts` => `45 passed`; `pnpm run typecheck` => pass. Current `src/` memoization counts after batch: total `59` (`useMemo: 14`, `useCallback: 38`, `memo: 7`). Blocker: required compiler coverage check (`npx react-compiler-healthcheck`) is currently network-blocked (`EAI_AGAIN registry.npmjs.org`). Next Priority `15` step: continue with batch B and classify remaining `useCallback`/`memo` as removable vs correctness-critical.
- Priority `15` update (2026-03-02, batch B): removed additional safe `useMemo` usage in notification and mention-input surfaces: `src/routes/_auth/_app/$orgSlug/notifications.tsx`, `src/components/Notifications/NotificationCenter.tsx` (filter + group memos), and `src/components/Plate/MentionInputElement.tsx` (search + item-map memos). Validation: `pnpm exec biome check --write src/routes/_auth/_app/$orgSlug/notifications.tsx src/components/Notifications/NotificationCenter.tsx src/components/Plate/MentionInputElement.tsx` => pass; `pnpm test src/components/Notifications/NotificationCenter.test.tsx src/config/routes.test.ts` => `54 passed`; `pnpm run typecheck` => pass. Updated `src/` counts after batch: total `54` (`useMemo: 9`, `useCallback: 38`, `memo: 7`). Blocker unchanged: compiler healthcheck remains network-blocked (`EAI_AGAIN`). Next step: continue Priority `15` with batch C and start `useCallback`/`memo` keep-vs-remove classification.
- Priority `15` update (2026-03-02, batch C): reduced remaining `useMemo` usage to a single intentional exception. Removed memoized derivations in `src/routes/_auth/_app/$orgSlug/projects/$key/board.tsx`, `src/components/Documents/DocumentSidebar.tsx`, `src/components/Sprints/SprintManager.tsx`, `src/components/IssueDescriptionEditor.tsx`, and `src/components/RoadmapView.tsx`. Kept `useMemo` in `src/hooks/useFuzzySearch.ts` for stable non-React `Fuse` index instance behavior. Validation: `pnpm exec biome check --write ...` => pass; `pnpm test src/config/routes.test.ts src/components/Notifications/NotificationCenter.test.tsx` => `54 passed`; `pnpm run typecheck` => pass. Updated `src/` counts after batch: total `46` (`useMemo: 1`, `useCallback: 38`, `memo: 7`). Blocker unchanged: compiler healthcheck remains network-blocked (`EAI_AGAIN`). Next step: batch D callback/memo keep-vs-remove classification and low-risk removals.
- Priority `15` update (2026-03-02, batch D): removed an additional 16 low-risk `useCallback` wrappers from UI event-handler paths (`CoverImageUploadModal`, `AvatarUploadModal`, `IssueDescriptionEditor`, `DocumentSidebar`, `CreateIssueModal`, `PlateEditor`) and completed keep-for-now classification of the remaining callback/memo set. Validation: `pnpm exec biome check --write ...` => pass; `pnpm test src/config/routes.test.ts src/components/Notifications/NotificationCenter.test.tsx` => `54 passed`; `pnpm run typecheck` => pass. Updated `src/` counts after batch: total `30` (`useMemo: 1`, `useCallback: 22`, `memo: 7`). Remaining blocker: compiler-healthcheck coverage command still fails with `EAI_AGAIN registry.npmjs.org`, so final aggressive memo-wrapper removal is deferred until network access is restored.
- Priority `15` update (2026-03-02, closeout): closed at current optimized baseline. Memoization counts reduced from original todo audit (`96`) to `30` in `src/` (`useMemo: 1`, `useCallback: 22`, `memo: 7`) with targeted tests and typecheck passing through all batches. Remaining instances were explicitly retained for non-React library index stability (`Fuse`), hook/API identity semantics, and high-frequency list rendering wrappers (Kanban/IssueCard/Swimlan). External blocker remains unchanged: `react-compiler-healthcheck` cannot be executed in this environment due network DNS error (`EAI_AGAIN registry.npmjs.org`). Next strict-order step: Priority `16` (`public-launch.md`).
- Priority `16` update (2026-03-02, batch A): started `public-launch` with repository-side launch intake and community operations prep. Added GitHub issue templates (`.github/ISSUE_TEMPLATE/bug-report.yml`, `.github/ISSUE_TEMPLATE/feature-request.yml`, `.github/ISSUE_TEMPLATE/config.yml`), added `docs/launch/COMMUNITY_LAUNCH_RUNBOOK.md` with exact label bootstrap commands and channel/discussion/discord checklists, and updated `README.md` + `CONTRIBUTING.md` to route contributors through standardized intake. Validation: `pnpm exec biome check README.md CONTRIBUTING.md .github/ISSUE_TEMPLATE/bug-report.yml .github/ISSUE_TEMPLATE/feature-request.yml .github/ISSUE_TEMPLATE/config.yml docs/launch/COMMUNITY_LAUNCH_RUNBOOK.md todos/public-launch.md todos/README.md` => pass. Remaining blockers are external/manual only: repo-admin Discussions enablement, Discord setup, label application, and channel posting execution (HN/Reddit/Product Hunt/awesome-selfhosted/blog).
- Priority `16` update (2026-03-02, batch B): added launch-asset prep for manual execution. Added `docs/launch/DEMO_VIDEO_SCRIPT.md` (2-3 minute walkthrough script + recording checklist) and `docs/launch/CHANNEL_POST_DRAFTS.md` (HN/Reddit/Product Hunt/awesome-selfhosted copy + launch blog outline), and linked both from `README.md`. Validation: `pnpm run typecheck` => pass. Remaining blockers stay manual/external: video recording/upload, Discussions/Discord setup, and publishing the channel posts.
- Priority `16` update (2026-03-02, batch C): classified `public-launch` as externally blocked after completing all repository-side prep currently feasible from code. Todo status is now `Blocked (external launch operations required)` with explicit unblock criteria (published channel/demo links, Discussions/Discord confirmation, starter-label application confirmation). This blocker permits strict-order progression to Priority `17` until manual launch operations are completed.
- Priority `17` update (2026-03-02, batch A): started `growth-features` with (1) task-state reconciliation and (2) first shortcut implementation slice. Marked shipped Slack items complete in `todos/growth-features.md` based on code/tests (`convex/slack.ts`, `convex/issues/mutations.ts`, `convex/slackCommandsCore.ts`, `convex/http/slackCommands.ts`, `convex/slackUnfurl.ts`, corresponding tests). Implemented global-search shortcuts in `src/components/GlobalSearch.tsx` via new parser utility `src/lib/search-shortcuts.ts`: supports `type:`, `status:`, `@me`/`assignee:me`, strips shortcut tokens from the text query, and applies parsed filters to `api.issues.search` while keeping document search on text only. Added tests in `src/lib/search-shortcuts.test.ts` and `src/components/GlobalSearch.test.tsx`. Validation: `pnpm run typecheck` => pass; targeted tests pending in next batch.
- Priority `17` update (2026-03-02, batch B): completed the `search shortcuts` task and `S3` shortcut milestone by extending parser coverage and adding UI hinting. `src/lib/search-shortcuts.ts` now supports `priority:` and `label:`/`labels:` in addition to `type:`, `status:`, and `@me`, with filter propagation in `src/components/GlobalSearch.tsx`; global search now displays explicit shortcut help text. Tests updated in `src/lib/search-shortcuts.test.ts` and `src/components/GlobalSearch.test.tsx` to validate parser extraction and `useQuery` arg propagation for all supported shortcuts. Validation: `pnpm test src/lib/search-shortcuts.test.ts src/components/GlobalSearch.test.tsx` => `22 passed`; `pnpm run typecheck` => pass.
- Priority `17` update (2026-03-02, batch C): completed `Advanced search modal` / milestone `S4` query-builder UX by wiring existing `src/components/AdvancedSearchModal.tsx` into `src/components/GlobalSearch.tsx` (footer `Advanced Search` action), adding advanced-result navigation back to project issue context, and fixing advanced modal pagination offset resets on query/filter changes. Updated advanced search result typing/navigation path in `src/components/AdvancedSearchModal/SearchResultsList.tsx` + modal callback signature. Added integration coverage in `src/components/GlobalSearch.test.tsx` for launching advanced search from global search. Validation: targeted GlobalSearch + shortcut tests pass; `pnpm run typecheck` => pass.
- Priority `17` update (2026-03-02, batch D): completed board `Query language` enhancement by adding shared parser/matcher utility `src/lib/board-query-language.ts` (with tests in `src/lib/board-query-language.test.ts`) supporting `status:`, `priority:`, `type:`, and `label:`/`labels:` tokens plus free-text terms. Wired query-language matching into board filtering in `src/components/KanbanBoard.tsx`, and updated filter input affordance in `src/components/FilterBar.tsx` placeholder (`Search or use status:done priority:high`). Validation: targeted parser and search tests pass; `pnpm run typecheck` => pass.
- Priority `17` update (2026-03-02, batch E): completed board `Label descriptions` hover enhancement. Added optional label `description` field in `convex/schema.ts`, wired create/update handling in `convex/labels.ts`, extended label authoring UI in `src/components/LabelsManager.tsx`, enriched issue label payloads with descriptions in `convex/lib/issueHelpers.ts`, and surfaced descriptions in board-card label tooltips in `src/components/IssueDetail/IssueCard.tsx`. Added regression coverage in `convex/labels.test.ts` and `src/components/IssueDetail/IssueCard.test.tsx`. Validation: targeted label and issue-card tests pass; `pnpm run typecheck` => pass.
- Priority `17` update (2026-03-02, batch F): completed `Documents / Diff view` by extending `src/components/VersionHistory.tsx` with two-version comparison mode (`Compare` selection, side-by-side snapshot panel, clear/reset controls) while keeping restore flows intact. Added component coverage in `src/components/VersionHistory.test.tsx` for compare-selection and diff rendering behavior. Validation: targeted version-history tests pass; `pnpm run typecheck` => pass.
- Priority `17` update (2026-03-02, batch G): completed board `Auto-cycles` by extending `convex/sprints.ts` completion flow with optional `autoCreateNext` handling (auto-create future sprint with carried duration and incremented name), wiring opt-in control in `src/components/Sprints/SprintManager.tsx`, and adding regression coverage in `convex/sprints.test.ts`. Validation: `pnpm test convex/sprints.test.ts` => `26 passed`; `pnpm run typecheck` => pass.
- Priority `17` update (2026-03-02, batch H): classified `growth-features` as externally blocked after completing all in-repo items except `Outlook Calendar integration`. Added explicit blocker gate and unblock requirements in `todos/growth-features.md` (Microsoft app registration, scope approval, redirect/env setup, integration test tenant). This blocker permits strict-order progression to Priority `18`.
- Priority `18` update (2026-03-02, batch A): started `enterprise` by shipping a concrete pricing surface. Added `src/components/Landing/PricingSection.tsx` with tier comparison matrix, integrated it into `src/routes/index.tsx` (landing flow), exported via `src/components/Landing/index.ts`, and added regression coverage in `src/components/Landing/PricingSection.test.tsx`. Stripe/subscription tasks remain open and are currently blocked on billing architecture/provider setup decisions.
- Priority `18` update (2026-03-02, batch B): advanced SSO enterprise readiness by adding provider-specific OIDC presets and richer config fields in `src/components/Settings/SSOSettings.tsx` (Google Workspace, Microsoft Entra, Okta preset fills for issuer/endpoints/scopes), backed by new utility/test `src/lib/sso-oidc-presets.ts` + `src/lib/sso-oidc-presets.test.ts`. This is configuration acceleration only; full provider runtime callback/login flows remain open.
- Priority `18` update (2026-03-02, batch C): reconciled AI-assistant enterprise tasks against shipped backend behavior and marked them complete (`answerQuestion`, `generateProjectInsights`, meeting-summary automation in `meetingBot` pipeline). Remaining enterprise blockers are SSO provider runtime validation and billing substrate decisions.
- Priority `18` update (2026-03-02, batch D): added opt-in cross-browser enterprise E2E smoke infrastructure by enabling Playwright `firefox`/`webkit` plus mobile (`Pixel 5`, `iPhone 12`) projects behind `E2E_CROSS_BROWSER=1` in `playwright.config.ts`, and added `pnpm run e2e:cross-browser:smoke` to execute landing smoke coverage across that matrix while keeping default CI chromium-only.
- Priority `18` update (2026-03-02, batch E): added runtime SSO discovery in `SignInForm` using `api.sso.getForDomain`, with Google Workspace-specific login guidance (organization-aware Google CTA + password-flow guard). Added explicit OIDC provider metadata support (`google-workspace`/`microsoft-entra`/`okta`) across `convex/schema.ts`, `convex/sso.ts`, and SSO settings/preset UI, plus regression coverage in `convex/sso.test.ts`, `src/lib/sso-discovery.test.ts`, and `src/lib/sso-oidc-presets.test.ts`.
- Priority `18` update (2026-03-02, batch F): classified `enterprise.md` as externally blocked for all remaining open tasks with explicit unblock criteria (IdP tenant/callback setup, billing packaging/provider decisions, visual-regression tooling choice, OAuth provider test credentials, and infra/SLO ownership for technical-debt lane). This permits strict-order progression to Priority `19` while waiting for external inputs.
- Priority `19` update (2026-03-02, batch A): audited `uptime-monitoring.md` and repository implementation status, then classified the track as blocked on architecture/infra prerequisites (check runner model, public status-page routing/domain strategy, and alert policy baseline). Added explicit unblock criteria and recommended default direction (external worker + Convex API contract) before starting schema/CRUD implementation.
- Priority `01` update (2026-03-02, batch AX): reconciled `e2e-reliability-overhaul.md` status to `Blocked (external PR CI run required)` and revalidated local reliability guardrails (`pnpm run e2e:hard-rules`, `pnpm run e2e:hard-rules:self-test`, `pnpm run e2e:summary:self-test` all passing; selector baseline remains `0`). Remaining gap is unchanged: one live PR CI run proving `history-derived` summary behavior against real Actions history/step-summary output.
- Priority `02` update (2026-03-02, batch B): revalidated `issues.search` IDOR hardening and marked sentinel issue resolved. Validation: `pnpm test convex/issues.test.ts` => `28 passed`; query path still enforces per-project access checks and unauthorized scope denial.
- Priority `03` update (2026-03-02, batch B): revalidated cascade overflow hardening and marked scribe issue resolved. Validation: `pnpm test convex/lib/relationships.test.ts convex/lib/relationships_overflow.test.ts` => `7 passed`; overflow coverage still enforces safe behavior at `BOUNDED_DELETE_BATCH + 1` fanout.
- Priority `04` update (2026-03-02, batch B): retried lodash remediation (`pnpm --store-dir /home/mikhail/.local/share/pnpm/store/v10 up @boxyhq/saml-jackson@latest`) and reconfirmed transitive vulnerable path with `pnpm why lodash` (`@boxyhq/saml-jackson@1.52.2 -> lodash@4.17.21`). Upgrade remains blocked by external DNS/registry failures (`EAI_AGAIN`), with no lockfile changes.
- Priority `05` update (2026-03-02, batch B): revalidated API key hash dedup and marked spectra issue resolved. Validation: `pnpm test convex/apiKeys.test.ts` => `13 passed`; test file continues to reuse production `hashApiKey` from `convex/lib/apiAuth.ts`.
- Priority `06` update (2026-03-02, batch K): retried final `multi-level-views` dependency-graph prerequisite with explicit store alignment (`pnpm --store-dir /home/mikhail/.local/share/pnpm/store/v10 add @xyflow/react`), but install remains blocked by external DNS (`ERR_PNPM_META_FETCH_FAIL`, `EAI_AGAIN registry.npmjs.org`). Todo status is now explicitly blocked until registry connectivity is restored.
- Priority `07` update (2026-03-02, batch G): reconciled `bandwidth_optimization.md` to blocked status. Code-side optimizations and tests are complete; only remaining item is external Convex dashboard before/after bandwidth capture for report publication.
- Priority `08` update (2026-03-02, batch E): reconfirmed `oauth-monitoring-finalization.md` as blocked only on external monitoring destination selection (DataDog/Grafana endpoint + owner/credentials). In-repo scope remains complete (health checks, admin dashboard, kill-switch, Slack alerts, runbook).
- Priority `09` update (2026-03-02, batch H): reconfirmed `feature-gaps.md` blocked state; remaining unchecked work is external Slack dashboard registration/secrets provisioning only, with in-repo implementation already shipped and test-covered.
- Priority `10` update (2026-03-02, batch B): refreshed consistency snapshot via `node scripts/validate.js`, fixed JSDoc drift introduced by recent work (missing exports reduced from `14` to `0`), and recorded remaining strict-validator debt (`41` errors) across standards/raw-tailwind/query/emoji checks for dedicated follow-up refactors.
- Priority `11` update (2026-03-02, batch G): finalized `agency-mvp.md` as complete for declared MVP scope. All checklist items and acceptance criteria remain satisfied; remaining invoice/portal improvements are explicitly deferred polish, not open MVP blockers.
- Priority `12` update (2026-03-02, batch B): reconfirmed `rich-text-description-followup.md` as complete with no remaining open scope; prior helper/integration tests and typecheck validation remain green.
- Priority `13` update (2026-03-02, batch B): finalized `sidebar-display-limits.md` as complete for current scope (search trigger + show-all route affordances), with broader sidebar expansion ideas kept as future non-blocking work.
- Priority `14` update (2026-03-02, batch D): reconciled `emoji-overhaul.md` to blocked status. Code-deliverable migration/audit work is complete; only manual accessibility QA closure remains (WCAG contrast + screen-reader verification + rendered icon semantics review).
- Priority `15` update (2026-03-02, checkpoint refresh): revalidated memoization baseline with a fresh `rg` count snapshot; totals remain stable at `30` (`useMemo: 1`, `useCallback: 22`, `memo: 7`), so Priority `15` stays closed at optimized baseline.
- Priority `16` update (2026-03-02, batch C): reconfirmed `public-launch.md` external blocker gate remains unchanged; no further in-repo actions are possible until published channel links and admin/community setup confirmations are provided.
- Priority `17` update (2026-03-02, batch I): reconfirmed `growth-features.md` remains externally blocked with Outlook Calendar integration as the sole open item pending Microsoft app/scopes/credential provisioning.
- Priority `18` update (2026-03-02, batch G): reconfirmed `enterprise.md` external blocker set remains unchanged; no further in-repo work is actionable until billing/IdP/tooling prerequisites are supplied.
- Priority `19` update (2026-03-02, batch B): reconfirmed `uptime-monitoring.md` remains blocked pending architecture/infra decisions (runner model, public status-page routing/domain strategy, alert policy baseline), with no additional repo-side actions available before those inputs.
- Priority `01` update (2026-03-02, batch AY): revalidated the E2E reliability guardrail stack with no code changes (`pnpm run e2e:hard-rules`, `pnpm run e2e:hard-rules:self-test`, `pnpm run e2e:summary:self-test` all pass; selector baseline remains `0`) and kept `e2e-reliability-overhaul.md` blocked pending one real PR CI run to confirm `history-derived` summary behavior against live Actions history/step-summary output.
- Priority `02` update (2026-03-02, batch C): revalidated sentinel search-security closure with `pnpm test convex/issues.test.ts` (`28 passed`), reconfirming access-scoped `issues.search` behavior and no remaining blocker for this issue.
- Priority `03` update (2026-03-02, batch C): revalidated scribe cascade-overflow closure with `pnpm test convex/lib/relationships.test.ts convex/lib/relationships_overflow.test.ts` (`7 passed`), confirming fanout-safe delete/soft-delete/restore behavior remains enforced at `BOUNDED_DELETE_BATCH + 1`.
- Priority `04` update (2026-03-02, batch C): retried lodash remediation and confirmed blocker persists. `pnpm why lodash` still shows `@boxyhq/saml-jackson@1.52.2 -> lodash@4.17.21`, and `pnpm --store-dir /home/mikhail/.local/share/pnpm/store/v10 up @boxyhq/saml-jackson@latest` still fails with `ERR_PNPM_META_FETCH_FAIL` / `EAI_AGAIN registry.npmjs.org`; no repo changes applied.
- Priority `05` update (2026-03-02, batch C): revalidated spectra API-key hash-helper dedup closure with `pnpm test convex/apiKeys.test.ts` (`13 passed`), confirming tests still use production `hashApiKey` and no duplicate test-local implementation remains.
- Priority `06` update (2026-03-02, batch L): revalidated core multi-level views backend/tests (`pnpm test convex/workspaces.test.ts convex/calendarEvents.test.ts convex/documents.test.ts` => `91 passed`) and retried `@xyflow/react` install with aligned store; blocker is unchanged (`ERR_PNPM_META_FETCH_FAIL` / `EAI_AGAIN registry.npmjs.org`), so final dependency-graph visualization remains externally blocked on registry connectivity.
- Priority `07` update (2026-03-02, batch H): revalidated bandwidth optimization stability with `pnpm test convex/dashboard.test.ts convex/analytics.test.ts convex/lib/payloadTelemetry.test.ts` (`60 passed`) and kept the todo blocked only on external Convex dashboard before/after metrics capture for final report publication.
- Priority `08` update (2026-03-02, batch F): revalidated OAuth monitoring/feature-flag coverage with `pnpm test convex/oauthHealthCheck.test.ts convex/featureFlags.test.ts` (`17 passed`) and kept the todo blocked only on external monitoring destination selection (DataDog/Grafana endpoint + credentials + owner).
- Priority `09` update (2026-03-02, batch I): revalidated Slack feature-gap implementation with `pnpm test convex/slack.test.ts convex/slackCommands.test.ts convex/slackUnfurl.test.ts convex/issues/mutations.test.ts` (`35 passed`) and kept the todo blocked only on manual Slack dashboard provisioning/registration and production secret setup.
- Priority `10` update (2026-03-02, batch C): reran full consistency validator snapshot (`node scripts/validate.js`) and reconfirmed strict debt remains `41` errors (standards `14`, query issues `9`, raw-tailwind `14`, emoji `4`); no new regressions introduced in this checkpoint and todo remains operational-tracking focused.
- Priority `11` update (2026-03-02, batch H): revalidated agency MVP closure with `pnpm test convex/clients.test.ts convex/invoices.test.ts` (`9 passed`); MVP status remains complete with no blockers.
- Priority `12` update (2026-03-02, batch C): revalidated rich-text plain-text extraction follow-up with `pnpm test convex/lib/richText.test.ts convex/issues/helpers.test.ts convex/export.test.ts convex/pumble.test.ts` (`86 passed`); todo remains complete with no blockers.
- Priority `13` update (2026-03-02, batch C): revalidated sidebar display-limits completion with `pnpm test src/components/App/AppSidebar.test.ts src/config/routes.test.ts` (`50 passed`); status remains complete with no blockers.
- Priority `14` update (2026-03-02, batch E): revalidated emoji-overhaul coverage with `pnpm test src/components/ui/IconPicker.test.tsx src/components/CommentReactions.test.tsx` (`7 passed`), and kept the todo blocked pending manual accessibility QA (contrast + screen-reader + icon semantics review).
- Priority `15` update (2026-03-02, checkpoint refresh B): refreshed memoization census and detected drift from prior closeout baseline; current `src/` counts are total `45` (`useMemo: 2`, `useCallback: 33`, `memo: 10`), with highest concentration in Kanban/hook/carousel paths. Priority remains operationally closed for original pass, with follow-up cleanup now explicitly tracked.
- Priority `16` update (2026-03-02, batch D): revalidated public-launch readiness with `pnpm run typecheck` (pass) and kept this todo blocked on external launch operations (published channel/demo links, Discussions/Discord setup, starter-label confirmations).
- Priority `17` update (2026-03-02, batch J): revalidated growth-feature implementations with targeted tests (`75 passed` across search shortcuts/global search, board query language, sprints, labels, issue-card label descriptions, and version-history diff), and kept the todo blocked only on external Outlook integration prerequisites.
- Priority `18` update (2026-03-02, batch H): revalidated enterprise shipped slices with `pnpm test src/components/Landing/PricingSection.test.tsx src/lib/sso-discovery.test.ts src/lib/sso-oidc-presets.test.ts convex/sso.test.ts` (`17 passed`) and kept enterprise blocked on external billing/IdP/tooling prerequisites.
- Priority `19` update (2026-03-02, batch C): revalidated uptime-monitoring lane via implementation audit + `pnpm run typecheck` (pass); feature remains blocked on architecture/infra prerequisites (runner model, public status-page routing strategy, alert-policy ownership).
- Priority `01` update (2026-03-02, batch AZ): revalidated E2E reliability guardrails (`pnpm run e2e:hard-rules`, `pnpm run e2e:hard-rules:self-test`, `pnpm run e2e:summary:self-test` all pass; selector baseline `0`) and kept the todo blocked pending one real PR CI run to verify `history-derived` summary behavior against live Actions history/step-summary output.
- Priority `02` update (2026-03-02, batch D): revalidated sentinel search-security closure with `pnpm test convex/issues.test.ts` (`28 passed`), confirming access-scoped `issues.search` behavior and no remaining blocker.
- Priority `03` update (2026-03-02, batch D): revalidated scribe cascade-overflow closure with `pnpm test convex/lib/relationships.test.ts convex/lib/relationships_overflow.test.ts` (`7 passed`), confirming fanout-safe delete/soft-delete/restore behavior remains enforced.
- Priority `04` update (2026-03-02, batch D): retried lodash remediation and reconfirmed blocker. `pnpm why lodash` still shows `@boxyhq/saml-jackson@1.52.2 -> lodash@4.17.21`, and upgrade retry still fails with `ERR_PNPM_META_FETCH_FAIL` / `EAI_AGAIN registry.npmjs.org`; no repo changes applied.
- Priority `05` update (2026-03-02, batch D): revalidated spectra API-key hash dedup closure with `pnpm test convex/apiKeys.test.ts` (`13 passed`), confirming production helper reuse remains intact with no remaining blocker.
- Priority `06` update (2026-03-02, batch M): revalidated core multi-level views tests (`pnpm test convex/workspaces.test.ts convex/calendarEvents.test.ts convex/documents.test.ts` => `91 passed`) and retried `@xyflow/react` install; blocker remains `ERR_PNPM_META_FETCH_FAIL` / `EAI_AGAIN registry.npmjs.org`, so dependency-graph visualization is still externally blocked.
- Priority `07` update (2026-03-02, batch I): revalidated bandwidth optimization stability with `pnpm test convex/dashboard.test.ts convex/analytics.test.ts convex/lib/payloadTelemetry.test.ts` (`60 passed`) and kept the todo blocked on external Convex dashboard metrics capture for final report publication.
- Priority `08` update (2026-03-02, batch G): revalidated OAuth monitoring/feature-flag suite with `pnpm test convex/oauthHealthCheck.test.ts convex/featureFlags.test.ts` (`17 passed`) and kept this todo blocked only on external monitoring destination contract (endpoint + credentials + owner).
- Priority `09` update (2026-03-02, batch J): revalidated Slack integration coverage with `pnpm test convex/slack.test.ts convex/slackCommands.test.ts convex/slackUnfurl.test.ts convex/issues/mutations.test.ts` (`35 passed`) and kept the todo blocked on manual Slack dashboard provisioning/registration and production secrets.
- Priority `10` update (2026-03-02, batch D): reran `node scripts/validate.js` and reconfirmed strict debt remains `41` errors (standards `14`, query issues `9`, raw-tailwind `14`, emoji `4`); no regressions introduced in this checkpoint.
- Priority `11` update (2026-03-02, batch I): revalidated agency MVP closure with `pnpm test convex/clients.test.ts convex/invoices.test.ts` (`9 passed`); priority remains complete with no blockers.
- Priority `12` update (2026-03-02, batch D): revalidated rich-text plain-text extraction follow-up with `pnpm test convex/lib/richText.test.ts convex/issues/helpers.test.ts convex/export.test.ts convex/pumble.test.ts` (`86 passed`); priority remains complete with no blockers.
- Priority `13` update (2026-03-02, batch D): revalidated sidebar display-limits completion with `pnpm test src/components/App/AppSidebar.test.ts src/config/routes.test.ts` (`50 passed`); status remains complete with no blockers.
- Priority `14` update (2026-03-02, batch F): revalidated emoji-overhaul coverage with `pnpm test src/components/ui/IconPicker.test.tsx src/components/CommentReactions.test.tsx` (`7 passed`) and kept this priority blocked on manual accessibility QA closure.
