# Agent Prompts (post-Jules)

This directory replaces the old `.jules` setup.

- `prompts/` — role prompts used by tmux/manual orchestration
- `journal/` — preserved historical agent journals moved from `.jules`
- `RULES.md` — shared guardrails for orchestration agents

Legacy `.jules` was removed after migration.
